import Vue from 'vue'
import App from './App.vue'

Vue.config.productionTip = false

new Vue({
  render: h => h(App),
}).$mount('#app')

// Un-comment the code below to integrate Vuex 
// and add global data store to your app: 
/*
import store from './store'

const preloaded = window.__PRELOADED__

new Vue({
  store: store(preloaded),
  render: h => h(App),
}).$mount('#app')
*/

