import Vue from 'vue'
import Vuex from 'vuex'
import { profiles } from './modules'

Vue.use(Vuex)

export default (preloaded = {}) => {

    return new Vuex.Store({
        modules: {
            profiles: profiles(),
        }
    })

}